name: Bilalian
options:
  bundleIdPrefix: com.ibnseddik
packages:
  Adhan:
    url: https://github.com/batoulapps/adhan-swift
    branch: main
targets:
  Bilalian:
    type: application
    platform: tvOS
    deploymentTarget: "26.0"
    sources:
      - Sources
    resources:
      - Sources/Resources
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.ibnseddik.bilalian
    info:
      path: Sources/Info.plist
      properties:
        CFBundleDisplayName: Bilalian
        LSRequiresIPhoneOS: false
        UISupportedInterfaceOrientations~tvOS:
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        NSLocationWhenInUseUsageDescription: "Your location is used locally to calculate accurate prayer times for your area."
        NSLocationUsageDescription: "Your location is used locally to calculate accurate prayer times for your area."
    dependencies:
      - package: Adhan
  BilalianTests:
    type: bundle.unit-test
    platform: tvOS
    sources:
      - Tests
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
    dependencies:
      - target: Bilalian
